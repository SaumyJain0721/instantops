import type { StatusDistribution } from '@/api/types'
import { BOOKING_STATUS_CONFIG, STATUS_CHART_COLORS } from '@/lib/utils'
export function StatusDonut({ data }: { data: StatusDistribution[] }) {
  const total=data.reduce((s,d)=>s+d.count,0)||1; let cursor=0
  const stops=data.map(d=>{const start=cursor; cursor += d.count/total*100; return `${STATUS_CHART_COLORS[d.status] ?? '#94a3b8'} ${start}% ${cursor}%`}).join(', ')
  return <div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-3"><h3 className="text-sm font-semibold text-slate-900">Status Distribution</h3><p className="mt-0.5 text-xs text-slate-500">All-time breakdown</p></div><div className="flex flex-col items-center gap-5 sm:flex-row sm:justify-center"><div className="relative h-44 w-44 shrink-0 rounded-full" style={{background:`conic-gradient(${stops})`}}><div className="absolute inset-7 flex flex-col items-center justify-center rounded-full bg-white"><span className="text-2xl font-bold text-slate-900">{total.toLocaleString('en-IN')}</span><span className="text-[11px] text-slate-400">bookings</span></div></div><div className="w-full max-w-xs space-y-2">{data.map(d=><div key={d.status} className="flex items-center justify-between text-xs"><span className="flex items-center gap-2"><span className="h-2.5 w-2.5 rounded-full" style={{background:STATUS_CHART_COLORS[d.status] ?? '#94a3b8'}} />{BOOKING_STATUS_CONFIG[d.status]?.label ?? d.status}</span><span className="font-semibold text-slate-800">{d.count} <span className="font-normal text-slate-400">({d.percentage.toFixed(1)}%)</span></span></div>)}</div></div></div>
}
