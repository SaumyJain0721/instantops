# Local development can leave this unset because Vite proxies /api to localhost:8080.
# For Vercel production, set this to the deployed Spring Boot backend base API URL.
# Example: https://your-backend.example.com/api
VITE_API_URL=
